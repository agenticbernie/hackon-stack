# Context engineering

Context acquisition uses progressive disclosure: preferred project entrypoints
first, then bounded source/config files to a depth and item limit, then scored
prior learning. Repository files are labelled untrusted and the adapter is
instructed not to follow embedded instructions that conflict with policy.

The selected paths and knowledge IDs are persisted in each run so context
selection is observable and debuggable.
