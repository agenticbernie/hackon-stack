# Compound learning

Successful runs write a redacted learning entry containing the cycle objective,
verified evidence count, stages, and workflow tags. Search uses a deliberately
small retrieval boundary: token overlap across title, content, and tags.

This is an upgradeable seam for embeddings, a database, or team memory. The
current implementation does not claim semantic retrieval or automatic causal
inference.
